"""Automatizacion de aplicar (Pilar 3).

Regla de oro: MCP = descubrir/leer (no envia); Extension = aplicar/submit asistido
(el usuario revisa y pulsa Enviar); Playwright = submit automatico opcional OFF;
Manual = fallback universal. Verbos separados discover() / apply().
"""
